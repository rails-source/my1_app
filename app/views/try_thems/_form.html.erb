<%= form_for(@try_them) do |f| %>
  <% if @try_them.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@try_them.errors.count, "error") %> prohibited this try_them from being saved:</h2>

      <ul>
      <% @try_them.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
